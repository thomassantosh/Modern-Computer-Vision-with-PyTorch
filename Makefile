install:
	#conda create -n cvision python=3.8 -y; conda activate cvision
	# pip install torch-snippets
	pip install notebook
	pip install torch-snippets
	# pip install fastapi
	# pip install uvicorn
	# pip install aiofiles
	# pip install jinja2
	# pip install flake8
